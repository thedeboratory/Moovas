/**
 * OAuth routes — retired. Email+password auth is used instead.
 * This file is kept as a stub so imports don't break.
 */
import type { Express } from "express";
export function registerOAuthRoutes(_app: Express): void {
  // No-op: Manus OAuth has been replaced with email+password auth.
}
